# Production Environment Configuration
# Used when deploying to Render or other cloud providers

# ============================================
# CORE APPLICATION SETTINGS
# ============================================

# Environment type (development, staging, production)
ENVIRONMENT=production

# Secret key for JWT tokens (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
# CHANGE THIS to a random 32+ character string!
SECRET_KEY=your-super-secret-key-min-32-characters-change-this

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ============================================
# DATABASE CONFIGURATION
# ============================================

# PostgreSQL Database URL for production
# Format: postgresql://username:password@hostname:5432/database_name
DATABASE_URL=postgresql://your_db_user:your_db_password@your_host:5432/devapply_db

# Alternative for SQLite (local development only)
# DATABASE_URL=sqlite:///./test.db

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# Frontend URL (where your React app is hosted)
# Example: https://devapply-frontend.onrender.com
FRONTEND_URL=https://devapply-frontend.onrender.com

# ============================================
# EMAIL CONFIGURATION (Gmail)
# ============================================

# Gmail address for sending notifications
SMTP_USER=your-email@gmail.com

# Gmail app-specific password (NOT your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_PASS=your-16-character-app-password

# Email sender name
SMTP_FROM_NAME=DevApply

# ============================================
# TELEGRAM CONFIGURATION (Optional)
# ============================================

# Telegram Bot Token (optional - leave blank if not using)
# Get from: https://t.me/botfather
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# ============================================
# RENDERING/BUILD CONFIGURATION
# ============================================

# Python version
PYTHON_VERSION=3.11.0

# Node version for frontend
NODE_VERSION=18

# ============================================
# LOGGING & MONITORING
# ============================================

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Enable debug mode (False in production!)
DEBUG=False

# ============================================
# SERVER CONFIGURATION
# ============================================

# Port (Render uses $PORT environment variable)
PORT=8000

# Workers for production
WORKERS=4

# ============================================
# CORS CONFIGURATION
# ============================================

# Allowed origins (comma-separated)
ALLOWED_ORIGINS=https://devapply-frontend.onrender.com,http://localhost:3000,http://localhost:5173

# ============================================
# JOB SCHEDULER CONFIGURATION
# ============================================

# Enable job scheduler
SCHEDULER_ENABLED=True

# Job scraping interval (hours)
SCHEDULER_INTERVAL_HOURS=6

# ============================================
# NOTES FOR DEPLOYMENT
# ============================================

# 1. REPLACE ALL "your-*" VALUES WITH ACTUAL VALUES
# 2. GENERATE A NEW SECRET_KEY (see above)
# 3. SET DATABASE_URL FROM YOUR RENDER DATABASE
# 4. SET FRONTEND_URL FROM YOUR FRONTEND DEPLOYMENT
# 5. SET SMTP_USER AND SMTP_PASS FOR EMAIL NOTIFICATIONS
# 6. STORE THIS FILE SECURELY (never commit to git)
# 7. USE .env FILE LOCALLY, RENDER ENV VARS FOR PRODUCTION

# Example SECRET_KEY generation:
# Windows PowerShell: [Convert]::ToBase64String([byte[]] (1..32 | % { Get-Random -Max 256 }))
# Linux/Mac: openssl rand -base64 32
